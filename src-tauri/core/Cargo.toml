[package]
name = "winget-core"
version = "1.5.0"
description = "Pure parsing and validation logic for WinGet System Manager (no Tauri)"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
once_cell = "1"
regex = "1"
tracing = "0.1"

[dev-dependencies]
serde_json = "1"
